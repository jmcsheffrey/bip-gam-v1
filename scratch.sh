# below checks that GAM is working, it will also prompt for upgrade if one in is available
#../bin/gam/gam info domain
# below downloads new version & runs install.  if the project is already setup, do not run that part when asked
#bash <(curl -s -S -L https://git.io/install-gam)
