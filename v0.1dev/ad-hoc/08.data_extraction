-- for using data from users table.  might be better to do that instead of "04.determine_updates"

-- ****************************************************
-- NAVIANCE data
-- ****************************************************

select
    unique_id,
    expected_grad_year,
    last_name,
    'SSCPSHigh' as campus_id,
    first_name,
    middle_name,
    referred_to_as as nickname,
    gender,
    
  from users
  order by
